//Sum of n natural numbers

#include<stdio.h>
int main()
{
    int n , N ,sum;
    for(n=1; N>=n; n++)
    sum +=n;
    printf("\nSum of %d natural numbers is %d: ", n, sum);
    scanf("%d",&N);
    return 0;
}