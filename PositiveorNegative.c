//a number is positive or negative

#include<stdio.h>
int main()
{
    int a;
    printf("Number a is ");
    scanf("%d", &a);
    if ( a > 0)
    printf("a is positive");
    else
    printf("a is negative");
    return 0;
}